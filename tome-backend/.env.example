# Database Configuration
# Update these values to match your PostgreSQL database
# Works with local databases, Docker, or managed database services

# Example for local database:
DB_URL=jdbc:postgresql://localhost:5432/tomedb
DB_USER=myuser
DB_PASSWORD=secret

# Example for managed database (DigitalOcean, AWS RDS, etc.):
# DB_URL=jdbc:postgresql://your-db-host.example.com:25060/defaultdb?sslmode=require
# DB_USER=doadmin
# DB_PASSWORD=your-database-password

# Mail Configuration (for email verification)
# Gmail example - you'll need to generate an App Password
# See: https://support.google.com/accounts/answer/185833
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password

# JWT Secret (generate a secure random string)
# You can generate one with: openssl rand -hex 32
JWT_SECRET=your-jwt-secret-key-change-this-in-production

# Service URLs (update based on your deployment)
# For Docker Compose, use container names
# For local development, use localhost
CONTENT_SERVICE_URL=http://content:8080/
AUTH_SERVICE_URL=http://auth:8082/

# For local development (uncomment and use these instead):
# CONTENT_SERVICE_URL=http://localhost:8080/
# AUTH_SERVICE_URL=http://localhost:8082/

# Database Configuration
DB_URL=jdbc:postgresql://localhost:5432/tomedb
DB_USER=myuser
DB_PASSWORD=secret

# Mail Configuration (Gmail example)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-specific-password

# For Gmail:
# 1. Enable 2-factor authentication
# 2. Generate an app-specific password
# 3. Use that password here

# Frontend URL
FRONTEND_URL=http://localhost:8081

# JWT Configuration
# IMPORTANT: Change this in production! Must be base64 encoded and at least 256 bits
# You can generate a secure key with: openssl rand -base64 32
JWT_SECRET=
